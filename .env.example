# ======================================================
# PROJECT CONFIGURATION
# ======================================================
PROJECT_NAME=BizTrack
PROJECT_URL=https://github.com/UnderWeb/biztrack-api
PROJECT_EMAIL=mail@gmail.com

# ======================================================
# ENVIRONMENT CONFIGURATION
# ======================================================
DJANGO_ENV=development
DJANGO_SETTINGS_MODULE=config.settings.${DJANGO_ENV}
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# ======================================================
# DJANGO SETTINGS
# ======================================================
DEBUG=True
SECRET_KEY=replace-with-your-own-secret-key
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
SERVER_PORT=8000

# ======================================================
# DATABASE (PostgreSQL)
# ======================================================
DB_USER=biztrack_user
DB_PASS=biztrack_password
DB_NAME=biztrack_db
DB_HOST=biztrack-postgres
DB_PORT=5432

# ======================================================
# REDIS (Cache & Celery Broker)
# ======================================================
REDIS_HOST=biztrack-redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_password
REDIS_DB_DEFAULT=1
REDIS_DB_CELERY=0

# ======================================================
# CELERY CONFIGURATION
# ======================================================
CELERY_TIMEZONE=UTC
CELERY_ENABLE_UTC=True
CELERY_ACCEPT_CONTENT=json
CELERY_TASK_SERIALIZER=json
CELERY_RESULT_SERIALIZER=json
CELERY_TASK_TRACK_STARTED=True
CELERY_TASK_TIME_LIMIT=300
CELERY_BEAT_SCHEDULER=django_celery_beat.schedulers:DatabaseScheduler

DJANGO_CELERY_BEAT_TZ_AWARE=True

# ======================================================
# EMAIL / MAILPIT (for local development)
# ======================================================
EMAIL_HOST=biztrack-mailpit
EMAIL_PORT=1025
EMAIL_USER=""
EMAIL_PASSWORD=""
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False
EMAIL_TIMEOUT=30
EMAIL_CONNECTION_MAX_RETRIES=3
EMAIL_BATCH_SIZE=90
EMAIL_MAX_ATTACHMENT_MB=50
DEFAULT_FROM_EMAIL=BizTrack <no-reply@biztrack.cl>

# ======================================================
# AWS S3 Storage
# ======================================================
AWS_ACCESS_KEY_ID=biztrack-mailpit
AWS_SECRET_ACCESS_KEY=1025
AWS_STORAGE_BUCKET_NAME=""
AWS_S3_REGION_NAME=us-east-1
AWS_STATIC_LOCATION=static
AWS_MEDIA_LOCATION=media

# ======================================================
# JWT
# ======================================================
JWT_ACCESS_LIFETIME_MINUTES=15
JWT_REFRESH_LIFETIME_DAYS=7
JWT_ROTATE_REFRESH_TOKENS=true
JWT_BLACKLIST_AFTER_ROTATION=true

# ======================================================
# TIMEZONE & LOCALIZATION
# ======================================================
LANGUAGE_CODE=es-cl
TIME_ZONE=America/Santiago
USE_I18N=True
USE_TZ=True
